# Runtime Backlog

- Define the versioned registration, capability-manifest, heartbeat, lease, evidence, cancellation, and revocation protocol.
- Harden the local Windows companion and add Linux/macOS parity.
- Implement unprivileged Docker, VPS, private-server, cloud-worker, and authorized network-agent adapters.
- Use short-lived credentials, mutual authentication, resource budgets, workspace confinement, and default-denied egress.
- Add emergency stop, lease reconciliation, offline behavior, enrollment rotation, and runtime isolation tests.

No runtime is connected until a live authenticated heartbeat verifies its identity and capabilities.
